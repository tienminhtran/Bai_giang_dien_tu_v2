services:
  minio:
    image: minio/minio:latest
    container_name: bgdt_minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    ports:
      - "${MINIO_PORT:-9000}:9000"       # S3 API
      - "${MINIO_CONSOLE_PORT:-9001}:9001" # Web Console
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Khoi tao bucket tu dong khi MinIO san sang
  minio-init:
    image: minio/mc:latest
    container_name: bgdt_minio_init
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 ${MINIO_ROOT_USER:-minioadmin} ${MINIO_ROOT_PASSWORD:-minioadmin};
      mc mb --ignore-existing local/${MINIO_BUCKET_RAW:-videos-raw};
      mc mb --ignore-existing local/${MINIO_BUCKET_VERSIONS:-videos-versions};
      mc mb --ignore-existing local/${MINIO_BUCKET_PUBLISHED:-videos-published};
      mc anonymous set download local/${MINIO_BUCKET_PUBLISHED:-videos-published};
      echo 'MinIO buckets initialized: videos-raw, videos-versions, videos-published';
      exit 0;
      "

volumes:
  minio_data:
    driver: local
