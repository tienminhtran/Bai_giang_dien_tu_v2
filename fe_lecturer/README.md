# fe_lecturer

Scaffolded Vite React app with TailwindCSS for the lecturer frontend.

Quick start:

```bash
cd fe_lecturer
npm install
npm run dev
```

Environment:
- Set `VITE_API_BASE_URL` in `.env` for API base URL if needed.
