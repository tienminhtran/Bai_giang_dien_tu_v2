const ASSESSMENT_SESSION = {
  LIST:   '/assessment-sessions',
  DETAIL: (id) => `/assessment-sessions/${id}`,
  CREATE: '/assessment-sessions',
  UPDATE: (id) => `/assessment-sessions/${id}`,
  DELETE: (id) => `/assessment-sessions/${id}`,
}

export default ASSESSMENT_SESSION
